import { EventTarget } from 'cc';

export const eventTarget = new EventTarget();

export const GAME_EVENTS =  {
    ON_PLAYER_MOVE:'ON_PLAYER_MOVE',
}

export const GAME_BOARDSIZE = {
    row:3,
    col:3
}